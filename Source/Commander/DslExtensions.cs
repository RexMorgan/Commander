using Commander.Registration.Dsl;
using Commander.Registration.Nodes;

namespace Commander
{
    public static class DslExtensions
    {
        //public static IEntityCommandExpression BeforeNew<TCommand, TEntity>(this IEntityCommandExpression expression)
        //    where TCommand : ICommand<TEntity>
        //    where TEntity : class
        //{
        //    return expression.BeforeNew(EntityCommandCall.CallFor<TCommand, TEntity>());
        //}

        //public static IEntityCommandExpression AfterNew<TCommand, TEntity>(this IEntityCommandExpression expression)
        //    where TCommand : ICommand<TEntity>
        //    where TEntity : class
        //{
        //    return expression.AfterNew(EntityCommandCall.CallFor<TCommand, TEntity>());
        //}
    }
}